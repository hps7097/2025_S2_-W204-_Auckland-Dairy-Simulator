	shader_type canvas_item;

	uniform float flash : hint_range(0.0, 1.0) = 0.0;

	void fragment() {
	    vec4 tex = texture(TEXTURE, UV);
	    // Add white on top instead of blending
	    vec4 added = tex + vec4(flash, flash, flash, 0.0);
	    COLOR = vec4(clamp(added.rgb, 0.0, 1.0), tex.a);
	}
